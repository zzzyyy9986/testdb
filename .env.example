# Порт веб-приложения на хосте
APP_PORT=8080

# MySQL (контейнер db)
MYSQL_ROOT_PASSWORD=root
DB_NAME=testdb
DB_USER=testdb
DB_PASSWORD=testdb

# Ключ AES для паролей админки (должен совпадать с дампом sql/testdb.sql)
AESKEY=aes_some_key_to_testdb777
